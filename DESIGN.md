# 극단 창파 웹 디자인 가이드

## 방향

극단 창파 웹사이트는 **낡은 공연 포스터**와 **무대 장비 콘솔**이 만나는 인상으로 설계한다.

첫인상은 디지털 서비스보다 공연장 입구에 붙은 인쇄물에 가깝다.  
그러나 인터랙션은 오래된 장비를 직접 만지는 듯 선명하고 즉각적이어야 한다.

핵심 문장:

> 레트로 포스터의 질감 위에서 무대 전원을 켜고, 창파의 장면 안으로 들어간다.

## 레퍼런스 해석

### 1. 레트로 공연 포스터

참고 이미지의 특징:

- 큰 숫자와 제목이 화면을 먼저 장악한다.
- 굵은 격자 안에 색면, 이미지, 정보 블록이 나뉜다.
- 종이색 바탕과 인쇄 얼룩이 시간감을 만든다.
- 색은 많지 않지만 크림, 오렌지 레드, 청록의 대비가 강하다.
- 장식보다 정보의 배치 자체가 그래픽이 된다.

창파 사이트 적용:

- 연혁의 연도, 작품 연도, 창단 연도는 큰 그래픽 요소로 다룬다.
- 대표작과 수상 내역은 포스터의 정보 블록처럼 단정한 칸으로 나눈다.
- 캘리그라피 로고와 공연 사진은 포스터의 주 이미지처럼 크게 쓴다.
- 질감은 과하게 빈티지하지 않게 얇게만 사용한다.

### 2. 오디오 콘솔 그래픽

참고 이미지의 특징:

- 검은 바탕 위 기하학적 장비가 정확한 각도로 배치된다.
- 버튼, 노브, 재생 기호가 인터랙션 언어로 읽힌다.
- 크림색 조작부와 붉은 포인트가 즉시 시선을 잡는다.
- 여백이 크고 형태가 단순하다.

창파 사이트 적용:

- 입장 버튼, 조명 스위치, 대표작 선택은 콘솔 조작처럼 보이게 만든다.
- `ON / OFF`, play, stop, 조명 상태 같은 작은 상태 표기를 활용한다.
- 패널 전환은 무대 장비가 작동하는 느낌으로 짧고 묵직하게 움직인다.
- 조작부는 둥근 pill, 선명한 트랙, 단단한 버튼 면으로 만든다.

## 디자인 키워드

- Retro performance poster
- Stage console
- Printed grid
- Bold year typography
- Theatrical light
- Tactile switch
- Simple geometry
- Restrained texture

## 시각 원칙

1. 화면을 카드 모음으로 만들지 않는다.  
   큰 포스터 판면과 정보 칸이 이어지는 구조로 본다.

2. 사진은 분위기 배경이 아니라 실제 공연 장면으로 쓴다.  
   배우, 무대, 조명, 오브제가 드러나야 한다.

3. 그래픽 포인트는 색면, 연도 숫자, 조작부에 집중한다.  
   불필요한 장식선과 효과는 줄인다.

4. 웹 텍스트는 단순한 고딕 계열을 기본으로 한다.  
   궁서체나 과한 붓글씨 느낌은 캘리그라피 이미지에만 남긴다.

5. 모바일에서 먼저 읽혀야 한다.  
   데스크탑은 같은 정보 구조를 넓게 펼치는 방향으로 확장한다.

## 색상

### 기본 팔레트

| 역할 | 색상 | 용도 |
| --- | --- | --- |
| Paper | `#EFE2BF` | 포스터 종이색, 밝은 정보 면 |
| Ink | `#0A0908` | 본문 배경, 인쇄 잉크 |
| Stage Black | `#171412` | 콘솔 패널, 버튼 트랙 |
| Poster Red | `#E6451C` | 강한 포인트, 선택 상태 |
| Teal | `#087E89` | 연도 블록, 보조 색면 |
| Dust | `#BDAE8A` | 보조 텍스트, 테두리 |
| Light | `#FFF1C9` | 조명, 활성 스위치 면 |

### 색 사용 규칙

- 한 화면의 주색은 `Ink`, `Paper`, 포인트 1색으로 제한한다.
- `Poster Red`와 `Teal`은 같은 비중으로 경쟁시키지 않는다.
- 조명 인터랙션에서는 `Light`를 짧게 드러내 활성 상태를 보여준다.
- 텍스처는 색을 더럽히는 수준이 아니라 종이 인상을 보태는 수준으로만 쓴다.

## 타이포그래피

### 기본

- 본문과 UI: 단순한 한글 산세리프
- 제목: 굵은 산세리프
- 숫자와 연도: 폭이 강한 디스플레이 숫자 또는 굵은 산세리프

### 적용 규칙

- `창조를 위한 파괴` 같은 핵심 문구는 한 줄로 읽히게 우선 배치한다.
- 한국어 제목은 글자 사이를 벌리지 않는다.
- 긴 작품명은 단어 흐름이 깨지지 않게 줄바꿈한다.
- 캘리그라피 로고는 이미지 자산으로만 사용하고, 본문 폰트 톤을 지배하지 않게 한다.

## 레이아웃

### 1. Entry Gate

첫 화면은 사이트 소개보다 **무대 입장 장면**이다.

- 화면 중앙에 포스터 프레임 또는 무대 창을 둔다.
- 안쪽에는 `Doodri 7` 공연 사진과 창파 캘리그라피를 둔다.
- 입장 스위치는 콘솔 전원처럼 배치한다.
- 전원을 켜면 첫 장면이 걷히고 실제 페이지가 드러난다.

### 2. Hero

안쪽 페이지의 첫 장면:

- 메인 공연 사진: `Doodri 7`
- 문구: `창조를 위한 파괴`
- 보조 문구는 짧게 유지
- 조명 스위치로 극단 소개 섹션을 밝히거나 이동시킨다.

### 3. About

- 긴 소개글보다 큰 문단 2개와 핵심 정보 3개를 먼저 보여준다.
- 창단, 대표, 지향은 포스터 정보 블록처럼 정리한다.
- 연출 철학은 별도 문장 블록으로 강하게 끊는다.

### 4. Selected Works

대표작:

1. 햄릿머신
2. 두드리 두드리
3. 푸른 관 속에 잠긴 붉은 여인숙

인터랙션:

- 모바일: 작품명을 누르면 해당 패널이 아래로 열린다.
- 데스크탑: 선택한 작품 패널이 가로로 확장된다.
- 활성 패널은 사진과 설명이 함께 드러난다.
- 비활성 패널은 제목과 연도만 남겨 탐색 가능하게 한다.

### 5. History And Awards

- 연혁은 단순 리스트보다 포스터의 연도 타이포그래피를 활용한다.
- 중요한 연도는 크기 또는 색면으로 강조한다.
- 수상은 작품 정보와 섞지 말고 짧은 정보 블록으로 분리한다.

## 인터랙션

### 스위치

- pill 형태의 `ON / OFF` 스위치를 기본 언어로 삼는다.
- 비활성 상태에서도 현재 상태가 명확해야 한다.
- 활성 상태는 밝은 슬라이더 면, 짧은 조명 효과, 상태 텍스트 변화로 표현한다.

### 대표작 패널

- 전환은 부드럽지만 느슨하지 않게 0.4초에서 0.8초 안에 끝낸다.
- 사진은 확대 효과를 작게만 준다.
- 패널이 열려도 텍스트가 사진을 읽기 어렵게 덮지 않게 한다.

### 조명

- 조명은 화면 전체를 번쩍이게 하지 않는다.
- 사진 밝기, 소개 텍스트 대비, 얇은 beam 효과를 함께 조절한다.
- 모션 감소 설정에서는 이동 애니메이션을 줄인다.

## 이미지 사용

- 메인 이미지는 `main.jpg`를 사용한다.
- 작품 이미지는 실제 공연 사진을 우선한다.
- 햄릿머신은 붉은 포스터 이미지를 쓰고, 작품 정보와 텍스트 소개는 펼쳐 읽는 패널로 분리한다.
- 사진 위에 과한 블러, 짙은 색 필터, 의미 없는 장식 그래픽을 얹지 않는다.

## 질감

질감은 레퍼런스의 종이 인상을 가져오되 화면을 낡게 만드는 목적이 아니다.

허용:

- 아주 약한 종이 노이즈
- 인쇄 얼룩처럼 보이는 미세한 밝기 차
- 날카롭지 않은 격자 테두리

피할 것:

- 읽기 어려운 얼룩
- 사진을 가리는 강한 grain
- 모든 섹션에 같은 빈티지 오버레이 반복

## 반응형 원칙

### Mobile

- 첫 화면에서 입장 스위치가 제목과 겹치지 않는다.
- 대표작은 세로 아코디언으로 읽힌다.
- 큰 연도 그래픽은 잘리더라도 의미가 남는 범위에서 사용한다.
- 터치 가능한 조작부는 충분히 크다.

### Desktop

- 포스터 그리드를 넓게 펼친다.
- 대표작은 가로 확장 패널로 보인다.
- 사진과 텍스트가 한 화면에 경쟁하면 사진을 먼저 살린다.
- 상단 내비게이션은 얇게 유지한다.

## 금지 사항

- 궁서체 느낌의 웹 본문 타이포그래피
- 마케팅 랜딩 페이지 같은 둥근 카드 남발
- 보라색 또는 파란색 그라데이션 중심의 현대 SaaS 톤
- 조명 효과를 빛 번짐 장식으로만 쓰는 것
- 실제 공연 사진보다 추상 장식을 먼저 보여주는 것

## 구현 체크리스트

- [ ] 첫 화면은 외부 입장 장면과 내부 소개 페이지가 분리되어 보인다.
- [ ] `Doodri 7`이 내부 hero의 메인 공연 이미지다.
- [ ] `창조를 위한 파괴`는 핵심 제목으로 한 줄 우선 배치된다.
- [ ] 스위치는 `ON / OFF` 상태를 즉시 읽을 수 있다.
- [ ] 대표작 3개 패널은 모바일과 데스크탑 모두 선택 가능하다.
- [ ] 본문 텍스트는 단순한 고딕 계열이다.
- [ ] 색상은 종이색, 잉크색, 붉은 포인트 또는 청록 포인트 중심으로 절제된다.
- [ ] 이미지와 텍스트가 작은 화면에서 겹치지 않는다.
